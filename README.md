# search-service

GET /services

param - search, asc, sort, page

POST /services

{
	"title": "<service name>",
	"description": "<description>",
	"detail": "<version detail>"
}

GET /services/:id

  
GET /services/:id/versions

  
PATCH /services/:id

  
DELETE /services/:id

